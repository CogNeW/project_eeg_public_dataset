function [goodEpochs] = RemoveBadEpochs(epochs, threshold)
% This function will take in a cell array of epochs and a threshold value.
% A new struct will be returned, with epochs containing values over the
% threshold being rejected.

for i = 1:length(epochs)
    disp('test');
end

end